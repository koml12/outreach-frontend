import React from "react";
import Typography from "@material-ui/core/Typography";

const EventNotFound = () => (
  <Typography variant="h3" align="center">
    Sorry, this event doesn't exist yet!
  </Typography>
);

export default EventNotFound;
